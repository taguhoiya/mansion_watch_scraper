# mansion_watch_scraper

## Run locally without docker

````bash
```python
python -m venv .venv
source .venv/bin/activate
make pip
````

## Run locally with docker

```bash
make run
```

## Enable ngrok

```bash
make ngrok
```
